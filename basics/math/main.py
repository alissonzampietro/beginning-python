# You'll need to import the lib math. It's a default library in python so you don't need to install it.
import math 


# It will basically round the number up
print(math.ceil(10.1)) # 11

# It will round the number down
print(math.floor(10.9)) # 10